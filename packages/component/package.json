{
  "name": "reactjs-view",
  "version": "0.2.2",
  "private": false,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!src",
    "!**/__tests__",
    "!tsconfig.json"
  ],
  "sideEffects": false,
  "scripts": {
    "compile": "tsc -b",
    "watch": "tsc -w",
    "clean": "rimraf ./dist && rimraf  tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "classnames": "^2.3.1",
    "react-jss": "^10.7.1",
    "reactjs-view-asset": "^0.2.0",
    "reactjs-view-core": "^0.2.0"
  }
}
