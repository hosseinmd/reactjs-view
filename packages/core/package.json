{
  "name": "reactjs-view-core",
  "version": "0.2.0",
  "private": false,
  "sideEffects": false,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!src",
    "!**/__tests__",
    "!tsconfig.json"
  ],
  "scripts": {
    "compile": "tsc -b",
    "watch": "tsc -w",
    "clean": "rimraf ./dist && rimraf  tsconfig.tsbuildinfo"
  }
}
