{
  "name": "reactjs-view-core",
  "version": "1.2.2",
  "private": false,
  "sideEffects": false,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "files": [
    "!dist",
    "src",
    "!**/__tests__",
    "!tsconfig.json"
  ],
  "scripts": {
    "compile": "tsc -b",
    "watch": "tsc -w",
    "clean": "rimraf ./dist && rimraf  tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "@use-gesture/react": "^10.1.6",
    "reactjs-view-share": "^1.2.0"
  }
}
