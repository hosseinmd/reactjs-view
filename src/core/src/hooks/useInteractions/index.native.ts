/**
 * @example
 *   const { isTabFocused, eventHandlers } = useInteractions();
 *
 *   return <button {...eventHandlers()} />;
 */
function useInteractions() {
  throw new Error("useInteractions: Not implemented in native");
}

export { useInteractions };
